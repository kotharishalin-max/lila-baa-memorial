# Copy to .env and fill in for local dev and deploy builds.
# The .env file is gitignored (contains infrastructure-specific config).

# Lambda API base URL for photo uploads (from .scripts/lila-baa-proxy/deploy.sh output)
VITE_UPLOAD_API_URL=https://YOUR-API-ID.execute-api.us-east-2.amazonaws.com/prod
